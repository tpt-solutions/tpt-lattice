[package]
name = "tpt-lattice-server"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Minimal Axum WebSocket server that broadcasts TPT Lattice ops to peers."
keywords = ["spreadsheet", "websocket", "crdt", "sync"]
categories = ["network-programming", "web-programming"]

[dependencies]
axum = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }
futures-util = "0.3"
tpt-lattice-crdt = { workspace = true }

[[bin]]
name = "tpt-lattice-server"
path = "src/main.rs"

[dev-dependencies]
tpt-lattice-crdt = { workspace = true }
tpt-lattice-core = { workspace = true }
